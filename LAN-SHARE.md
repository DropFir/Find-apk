# Find APK 局域网搜索

双击仓库根目录的 `start-lan-share.command`。首次启动会自动准备网页服务组件，
并安装为当前 macOS 用户的后台常驻服务，之后窗口会显示两条地址：

- 本机地址：只用于这台 Mac。
- 其他电脑地址：同一局域网中的电脑和手机使用这个地址。

启动窗口可以直接关闭。服务会在用户登录后自动启动，异常退出时自动重启。
队列和索引保存在 `.find-apk-share/`，服务日志保存在
`~/Library/Logs/Find-APK/`。

如需管理服务，可在项目目录运行：

```bash
.venv/bin/python tools/lan_service.py status
.venv/bin/python tools/lan_service.py start
.venv/bin/python tools/lan_service.py stop
.venv/bin/python tools/lan_service.py uninstall
```

网页会自动读取 `downloads/`，每 15 秒检查一次新增内容。只有安装包、WEBP
图标和 `developer.txt` 都存在，并通过现有 `validate_delivery.py` 校验的目录
才会显示。正在下载或尚未补齐的目录不会进入搜索结果，也不会被网页修改。

## 添加 APK 查找关键词

在网页的“关键词任务”中，每行输入一个应用名称，然后点击“加入关键词队列”。
一次可以批量提交任意数量的关键词。关键词会保存在这台 Mac 的本地队列中，
关闭网页或重启服务也不会丢失。

网页不再手动启动 Agent。Codex 中的“Find APK 可见队列”定时任务每 30 分钟读取一次，
每次按提交顺序领取最多 10 个关键词。任务队列使用横向表格展示，每页 20 条，
包含状态、提交时间、开始时间、查找耗时、尝试次数和结果说明。

队列状态含义：

- `等待处理`：尚未被 Agent 领取。
- `正在寻找`：已经进入当前批次。
- `等待重试`：上一轮异常结束，关键词已自动回到队列。
- `已经完成`：完整交付通过 `validate_delivery.py`。
- `付费已跳过`：官方当前页面明确要求购买。
- `无结果已跳过`：已经完成 2 轮不同的有效搜索，仍没有可交付的 Android 安装包。
- `iOS 应用`：工作人员确认目标只有 iOS 版本。
- `付费应用`：工作人员确认目标需要购买。
- `找不到`：工作人员确认没有可交付的 Android 安装包。

“无结果已跳过”和“付费已跳过”都可以编辑最终原因，再归类为“iOS 应用”、
“付费应用”或“找不到”；三类记录分别展示，也可以重新归类。

跳过记录右侧的“重新查找”会把该关键词退回待处理队列并清零无结果轮数。Agent
发现包名匹配的精确候选页后，队列会保存候选 URL 并禁止继续登记 `miss`；只有候选
明确失效并完成规定后备核验后，才能解除候选锁。

相同 URL 的重复失败、工具错误和没有取得完整响应的网络请求不会增加有效搜索次数。
第 1 轮无结果后 Agent 自动继续；第 2 轮仍无结果时自动跳过该关键词并处理后续
队列，不需要人工确认。

如果 ChatGPT 桌面应用没有运行，关键词仍会保存在队列里，并等待下一次定时读取。

## 每日制作队列

网页左侧的“每日制作队列”会把已经完整入库的应用稳定分成 A、B 两组，两位制作
人员各负责一列。点击“下载”会取得包含安装包、图标、`developer.txt` 和
`source.txt` 的 ZIP，同时该项目会在两台电脑上同步变灰，表示今天已经领取。源交付
目录会保留，因此接收端磁盘不足、网络中断或文件误删后，可直接点击“重新下载”。

下载操作不会删除或移动 `downloads/` 中的原文件。第二天生成新队列时，前一天
已经下载的项目不再显示；没有下载的项目会保留原分组并自动顺延到当天。相同目录
后续换成新的安装包版本时，会作为新的项目重新进入队列。

A、B 两列各自带有独立的日期选择器和前后一天按钮，可以让两个人分别查看不同
日期。页面会同时显示昨天的总数以及 A、B 两组昨天各自的数量。历史日期保留当日
快照，未完成的项目会显示为“未完成，已顺延”。

已经出现在指定 APKBA 制作监控中的应用，可以按真实 Android 包名同步到本地排除
清单。命中的关键词会从当前和历史制作队列隐藏，同关键词的新版本也不会重新进入；
这个操作只调整制作队列，不删除 `downloads/` 中的安装包。

局域网服务会每 30 秒检查 APKBA 制作监控。新的任务只要出现相同的真实 Android
包名，本地制作队列就自动变灰并显示“网站已完成”，不需要再次点击下载；该项目
第二天不会继续顺延。自动监控只更新状态，不自动删除本机安装包。

服务还会每 5 分钟读取 `https://www.apkba.com/apps-sitemap.xml`，它与公开搜索结果
使用同一批已发布应用数据。公开站或制作监控任一处出现相同真实包名，都会自动完成
本地队列项目；仍显示为普通“待下载”的项目表示两个来源均未命中。

默认端口是 `8765`，避免与 APK 下载后备服务占用的端口冲突。如需临时改为其他
端口，可在终端中运行：

```bash
FIND_APK_PORT=8090 ./start-lan-share.command
```

网页只负责保存和展示关键词，不启动隐藏的命令行 Agent。Codex 的
“Find APK 可见队列”自动唤醒会在指定任务中领取并处理。
